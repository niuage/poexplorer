require 'spec_helper'

describe Elastic::ItemSearch do

  describe "tire_search" do

    context "socket colors" do

      it "searching for R returns all items with a single red socket", :elastic do
        # item = create(:indexed_weapon_with_sockets)
        # weapon_search = create(:weapon_search, item: create(:item))

        # search = described_class.new(weapon_search)
        # tsearch = search.tire_search
      end

    end

  end

end
