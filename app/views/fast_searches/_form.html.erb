<%= simple_form_for @fast_search do |f| %>
  <%= f.input :query,
        as: :string,
        label: false,
        placeholder: "Search uniques",
        autocomplete: "off",
        spellcheck: false
  %>
  <%= f.submit "Search" %>
<% end %>
