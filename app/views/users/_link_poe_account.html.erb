<h2>Link your PoE account to this account</h2>

<div class="bordered-section text">
  <p>Open any of your threads on the official forum (<a href="http://www.pathofexile.com/forum">PathOfExile.com</a>), edit the first post, and add this token anywhere: <code><%= @token %></code></p>

  <% if false %>
  <%= simple_form_for :user, url: generate_token_user_path(@user) do |f| %>
    <%= f.submit "Generate token", class: "btn btn-success" %>
  <% end %>
  <% end %>
</div>

<div class="bordered-section text">
  <p>Enter the URL of the thread below, and validate.</p>

  <%= simple_form_for :user, url: validate_account_user_path(@user) do |f| %>
    <%= f.input :thread_url, input_html: { value: nil, class: "span12" } %>
    <%= f.submit "Validate", class: "btn btn-success" %>
  <% end %>
</div>

<div class="bordered-section text">
  <p></p>
</div>

