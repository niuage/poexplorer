# {
#   category: {
#     "the regexp": [node_ids]
#   }
# }

# node_ids.each do |node_id|

#   tree_node = TreeNode.find_by(uid: node_id)
#   tree_node.stats.each do |stat|
#     regexp = find_regexp(stat)
#     values = extract_values(stat, regexp)
#     category = find_category(stat_id)-

#     results[category]
#   end


# end
